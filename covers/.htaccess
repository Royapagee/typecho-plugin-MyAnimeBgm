# 图片缓存需要被浏览器直接访问，这里只关掉目录列表
Options -Indexes

<IfModule mod_authz_core.c>
    Require all granted
</IfModule>
<IfModule !mod_authz_core.c>
    Allow from all
</IfModule>

# 图片内容不会变，缓存久一点
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpeg "access plus 30 days"
</IfModule>
